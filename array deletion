#include<stdio.h>

main()
{
    int a[10],i,n;
    printf("Enter the size of an array");
    scanf("%d",&n);

    for(i=0;i<n;i++)
    {
        printf("Enter index %d elementa ",i);
        scanf("%d",&a[i]);
    }

    for(i=0;i<n;i++)
    {
        printf("\nElements at index %d = %d",i,a[i]);
    }
    int pos;
    printf("Enter the position you want to delete");
    scanf("%d",&pos);

    for(i=pos;i<n-1;i++)
    {
        a[i]=a[i+1];
    }
    n--;
    for(i=0;i<n;i++)
    {
        printf("\nElement in the array at index %d = %d",i,a[i]);
    }
}


output:
Enter the size of an array4
Enter index 0 elementa vamsi
Enter index 1 elementa Enter index 2 elementa Enter index 3 elementa
Elements at index 0 = -1
Elements at index 1 = -1
Elements at index 2 = 46
Elements at index 3 = 0Enter the position you want to delete
Element in the array at index 0 = -1
Element in the array at index 1 = 46
Element in the array at index 2 = 0
